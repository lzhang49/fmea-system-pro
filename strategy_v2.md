# 龙回头策略体系 v2（2026-08-11 扩展）

> 已有策略（v1）：龙回头主规则（10日翻倍+低标≤3板+回调确认+k=5信号+持有3日）
> 本次扩展：在主规则之上，新增7个**独立可执行、与主规则兼容**的子策略模块

---

## 体系全景

```
┌─────────────────────────────────────────────────────┐
│   龙回头策略体系 v2                                  │
├─────────────────────────────────────────────────────┤
│  主策略（v1）                                       │
│   S1 龙回头主规则 — 核心信号源                       │
├─────────────────────────────────────────────────────┤
│  风控类（本次新增）                                  │
│   S2 单笔硬止损（移动止损）                          │
│   S3 整体回撤熔断（暂停交易）                        │
│   S4 仓位集中度限制（防单票黑天鹅）                  │
├─────────────────────────────────────────────────────┤
│  收益优化类（本次新增）                              │
│   S5 信号强度分级（仓位分档）                        │
│   S6 持仓周期自适应（动态止盈）                      │
├─────────────────────────────────────────────────────┤
│  成本控制类（本次新增）                              │
│   S7 流动性与冲击成本过滤                            │
├─────────────────────────────────────────────────────┤
│  应急类（本次新增）                                  │
│   S8 黑天鹅/系统性风险（流动性踩踏应对）             │
└─────────────────────────────────────────────────────┘
```

---

## S1 龙回头主规则（已有）

| 项目 | 内容 |
|---|---|
| 入池 | 同花顺异常监控池（10个交易日累计涨幅≥100%） |
| 标的过滤 | 最高连板≤3（低标优先，排除7板+换手高标） |
| 入场 | 距峰值回撤确认≥10% + 峰值后第k=5日信号 |
| 买点 | 信号次日（T+1）开盘价 |
| 持有 | 3个交易日，收盘卖出 |
| 手续费 | 买入万3 + 卖出千1.3 |
| 历史表现 | 5年4.17x / 卡玛1.82 / 胜率64.7% / 单笔净超额+3.40% |

**S1 是其他所有子策略的"信号源"，必须开启。**

---

## S2 单笔硬止损（移动止损）— 风控类

| 项目 | 内容 |
|---|---|
| 优先级 | P0（最高） |
| 状态 | 🆕 新增 |
| 启用 | 默认 **ON** |
| 适用场景 | S1 持仓期间出现单票快速下跌，需要保护本金 |

**触发条件**（任一满足即触发）：
1. **绝对止损**：持仓浮亏 ≥ 7%（基于买入价）
2. **移动止损**：从买入后最高收盘价回撤 ≥ 5%（保住已有浮盈）
3. **跌停止损**：当日触及跌停板（盘中触发，次日开盘卖出）

**执行步骤**：
1. 盘中监控持仓盈亏（每日14:00 / 14:30 两个检查点）
2. 触发任一条件 → 在 `sim_pf.json` 中标记 `force_sell=true`
3. 次日 9:25 集合竞价后立即市价卖出

**参数配置**（`config.json` 可改）：
- `abs_stop_pct`: 7（绝对止损线）
- `trail_stop_pct`: 5（移动止损线）
- `check_times`: ["14:00", "14:30"]

**与 S1 关系**：
- ✅ **协同**：S1 持有的票被 S2 提前止损，避免继续下跌
- ⚠️ **互斥覆盖**：S2 止损时跳过 S6 动态止盈

**异常处理**：
- 跌停一字板无法卖出 → 列入次日第一笔卖出名单
- 连续3日跌停 → 自动降仓50%（转 S3 流程）

---

## S3 整体回撤熔断 — 风控类

| 项目 | 内容 |
|---|---|
| 优先级 | P0 |
| 状态 | 🆕 新增 |
| 启用 | 默认 **ON** |
| 适用场景 | 模拟盘/实盘出现连续亏损，整体回撤达到警戒线 |

**触发条件**：
- 模拟盘总资产从**历史最高点**回撤 ≥ **15%** → **暂停 S1 买新仓 5 个交易日**
- 回撤 ≥ **20%** → **清空所有持仓 + 暂停 S1 买新仓 10 个交易日**
- 回撤 ≥ **30%** → **永久停止自动交易**，只允许人工干预

**执行步骤**：
1. 每日15:30扫描时计算 `drawdown = (peak_equity - current_equity) / peak_equity`
2. 触发熔断 → 写入 `sim_pf.json` 的 `circuit_breaker` 字段
3. S1 build 时检查 `circuit_breaker` 状态，跳过建仓

**参数配置**：
- `soft_pause_pct`: 15
- `hard_pause_pct`: 20
- `hard_pause_days`: 10
- `liquidation_pct`: 30
- `cooldown_days`: 5（轻熔断冷却）

**与 S1 关系**：
- ⚠️ **强制覆盖**：S3 触发时 S1 即使有信号也不建仓
- 冷却期结束后自动恢复

**异常处理**：
- 熔断期间若市场连续上涨 → 仍按兵不动（纪律优先）

---

## S4 仓位集中度限制 — 风控类

| 项目 | 内容 |
|---|---|
| 优先级 | P1 |
| 状态 | 🆕 新增 |
| 启用 | 默认 **ON** |
| 适用场景 | 同时多只信号触发时，防止单票黑天鹅冲击整体 |

**触发条件**：
- 单只票持仓金额 ≤ 总资金 **30%**
- 单行业持仓金额 ≤ 总资金 **50%**（按通达信行业分类）
- 持仓总股票数 ≤ **3 只**（基于 S1 最优组合的实测上限）

**执行步骤**：
1. S1 build 准备建仓前先计算资金分配
2. 超过集中度限制 → 缩减超出部分，按比例等分到其他信号
3. 仅1只信号触发 → 即使100%资金也不算违规

**参数配置**：
- `max_single_pos_pct`: 30
- `max_industry_pct`: 50
- `max_holdings`: 3

**与 S1 关系**：
- ✅ **协同**：S4 是 S1 build 的**前置过滤器**
- ✅ **兼容**：5.5年回测最优组合就是"最多3只"，完全契合

**异常处理**：
- 资金量过小（< 1万元/只）→ 跳过该信号（无法整手买入）

---

## S5 信号强度分级 — 收益优化类

| 项目 | 内容 |
|---|---|
| 优先级 | P2 |
| 状态 | 🆕 新增 |
| 启用 | 默认 **ON** |
| 适用场景 | 同时多只 S1 信号触发，需要给每只票分配不同仓位 |

**信号分级**（基于额外条件打分）：

| 等级 | 加分项 | 建议仓位（相对等分基线） |
|---|---|---|
| ⭐⭐⭐ S级（强信号） | 入池时最高连板=1 + 创业板 + 距峰值回撤15-30% | **1.5x** |
| ⭐⭐ A级（标准） | 满足 S1 主规则条件 | **1.0x**（基线） |
| ⭐ B级（弱信号） | 主板 + 距峰值回撤30-35%（接近失效区） | **0.5x** |
| ❌ 排除 | 7板+高标 / 跌停板信号 / 连续3日跌停 | 0 |

**执行步骤**：
1. 对所有 S1 信号打分分级
2. 计算加权仓位 = 总资金 × Σ权重
3. 每只票按权重比例买入

**参数配置**：
- `grade_s_multiplier`: 1.5
- `grade_a_multiplier`: 1.0
- `grade_b_multiplier`: 0.5

**与 S1 关系**：
- ✅ **协同**：S1 出信号 → S5 给信号打分 → 决定仓位大小
- ✅ **兼容**：分级=0 的信号会被排除，更严格的过滤

**异常处理**：
- 全为B级弱信号 → 全部用0.5x仓位（仍可建仓，但不重仓）
- 全为排除级 → 维持空仓

---

## S6 持仓周期自适应 — 收益优化类

| 项目 | 内容 |
|---|---|
| 优先级 | P2 |
| 状态 | 🆕 新增 |
| 启用 | 默认 **ON** |
| 适用场景 | S1 固定持有3日有时过早或过晚，需要动态调整 |

**自适应规则**：

| 情境 | 触发条件 | 操作 |
|---|---|---|
| **快速止盈** | 持仓第2日浮盈 ≥ 5% | 提前卖出（不等到第3日） |
| **延长持有** | 持仓第3日仍浮盈 ≥ 3% | 延长至第4日，观察是否继续涨 |
| **强制止损** | 触发 S2 → 立即离场 | 与 S2 联动 |
| **标准持有** | 其余情况 | 走 S1 默认3日规则 |

**执行步骤**：
1. 每日14:00 检查所有 S1 持仓浮盈
2. 触发快速止盈 → 标记 `early_sell=true`，次日开盘卖
3. 触发延长持有 → `extend_hold=true`，第4日再判断

**参数配置**：
- `quick_take_profit_pct`: 5（快速止盈线）
- `extend_profit_pct`: 3（延长持有的最低浮盈）
- `max_extend_days`: 1（最多延长1天）

**与 S1 关系**：
- ✅ **协同**：S6 是 S1 持有期的"动态调整"
- ⚠️ **被 S2 覆盖**：S2 触发时 S6 跳过

**回测预期**（基于 v1 主规则小幅改造）：
- 胜率 64.7% → 67-70%
- 平均净超额 +3.40% → +3.8%
- 平均持仓周期 3.0日 → 2.8日

**异常处理**：
- 延长日触发跌停 → 不再延，次日开盘卖

---

## S7 流动性与冲击成本过滤 — 成本控制类

| 项目 | 内容 |
|---|---|
| 优先级 | P1 |
| 状态 | 🆕 新增 |
| 启用 | 默认 **ON** |
| 适用场景 | 小票/低流动性票，可能存在冲击成本吃掉全部收益 |

**触发条件**（任一满足即过滤掉该信号）：
1. **日均成交额** < 5000万（流动性枯竭）
2. **单笔买入金额** > 该股**日均成交额的 5%**（冲击成本过高）
3. **流通市值** < 5亿（小票A杀风险，回测验证过的小票风险）
4. **一字板信号**：信号日一字板无法成交 → 放弃

**执行步骤**：
1. S1 build 前先用通达信 `tdx_quotes` 拉每只信号的实时行情
2. 计算日均成交额（5日均值）
3. 触发任一条件 → 从信号列表移除

**参数配置**：
- `min_avg_amount_5d`: 50000000（5000万）
- `max_buy_pct_of_volume`: 0.05
- `min_float_market_cap`: 500000000（5亿）

**与 S1 关系**：
- ✅ **协同**：S1 信号 → S7 流动性过滤 → 减少无效交易
- ✅ **兼容**：过滤掉的信号本就不该买

**异常处理**：
- 行情接口失败 → 默认通过 S7（宁可多买不可错过）

---

## S8 黑天鹅/系统性风险 — 应急类

| 项目 | 内容 |
|---|---|
| 优先级 | P0（紧急触发） |
| 状态 | 🆕 新增 |
| 启用 | 默认 **ON** |
| 适用场景 | 突发重大事件：监管/战争/熔断/流动性踩踏 |

**触发条件**（任一满足）：
1. **大盘熔断**：沪深300当日跌幅 ≥ 7%
2. **系统性大跌**：沪深300两日累计跌幅 ≥ 10%
3. **监管黑天鹅**：管理层突发重大利空公告（需人工触发或新闻API检测）
4. **流动性踩踏**：持仓中 ≥ 2 只跌停 + 大盘跌 ≥ 3%

**执行步骤**：
1. 检测触发条件（实时监控 + 新闻扫描）
2. 立即清空所有 S1 持仓（次日开盘市价卖出）
3. 暂停 S1 买新仓 **20 个交易日**
4. 写入 `sim_pf.json` 的 `black_swan_active` 字段
5. 推送告警消息给用户（模拟盘阶段先做日志告警）

**参数配置**：
- `index_crash_pct`: 7（单日熔断线）
- `index_crash_2d_pct`: 10（两日累计线）
- `pause_days`: 20（冷却期）
- `panic_threshold_stocks`: 2（同时跌停持仓数）

**与 S1 关系**：
- ⚠️ **强制覆盖**：S8 触发时 S1 全部暂停
- ⚠️ **强制覆盖 S2/S3**：S8 是最高优先级
- ⚠️ **与 S3 协同**：S8 触发后也会更新 S3 的 peak 记录

**异常处理**：
- 熔断期结束后，重新计算 peak 起点（避免熔断前的虚高峰值）

---

## 策略优先级排序（执行时序）

```
每日15:30收盘扫描
    ↓
[1] S8 黑天鹅检测 ──触发──> 紧急清仓 + 长冷却
    ↓ 未触发
[2] S3 回撤熔断检查 ──触发──> 暂停S1，不建仓
    ↓ 未触发
[3] S1 龙回头主规则扫描 → 候选信号列表
    ↓
[4] S7 流动性过滤 → 过滤低流动性信号
    ↓
[5] S5 信号分级打分 → 给每只信号分S/A/B级
    ↓
[6] S4 仓位集中度限制 → 计算每只票可建仓金额
    ↓
[7] S1 build 建仓
    ↓
    ├─── 盘中14:00 / 14:30 ───┐
    │                         ↓
    │              [8] S2 单笔硬止损检查
    │                         ↓
    │              触发 → 次日开盘卖
    │                         ↓
    │              [9] S6 持仓周期自适应判断
    │                         ↓
    │              触发快速止盈/延长持有
    ↓
[10] 每日 15:30 持仓盈亏计算 → 输出 sim_portfolio.html
```

---

## 参数总表（config.json）

```json
{
  "S1_main": {
    "k_value": 5,
    "max_run_max": 3,
    "pullback_min_pct": 10,
    "hold_days": 3,
    "buy_fee_bps": 3,
    "sell_fee_bps": 13
  },
  "S2_stop_loss": {
    "enabled": true,
    "abs_stop_pct": 7,
    "trail_stop_pct": 5,
    "check_times": ["14:00", "14:30"]
  },
  "S3_circuit_breaker": {
    "enabled": true,
    "soft_pause_pct": 15,
    "hard_pause_pct": 20,
    "hard_pause_days": 10,
    "liquidation_pct": 30,
    "cooldown_days": 5
  },
  "S4_concentration": {
    "enabled": true,
    "max_single_pos_pct": 30,
    "max_industry_pct": 50,
    "max_holdings": 3
  },
  "S5_signal_grade": {
    "enabled": true,
    "grade_s_multiplier": 1.5,
    "grade_a_multiplier": 1.0,
    "grade_b_multiplier": 0.5,
    "score_chi_next": 1,
    "min_pullback": 15,
    "max_pullback": 30
  },
  "S6_dynamic_hold": {
    "enabled": true,
    "quick_take_profit_pct": 5,
    "extend_profit_pct": 3,
    "max_extend_days": 1
  },
  "S7_liquidity": {
    "enabled": true,
    "min_avg_amount_5d": 50000000,
    "max_buy_pct_of_volume": 0.05,
    "min_float_market_cap": 500000000
  },
  "S8_black_swan": {
    "enabled": true,
    "index_crash_pct": 7,
    "index_crash_2d_pct": 10,
    "pause_days": 20,
    "panic_threshold_stocks": 2
  }
}
```

---

## 互斥/协同矩阵

|  | S1 | S2 | S3 | S4 | S5 | S6 | S7 | S8 |
|---|---|---|---|---|---|---|---|---|
| **S1** | — | 协同 | 被覆盖 | 协同 | 协同 | 协同 | 协同 | 被覆盖 |
| **S2** | 协同 | — | 协同 | — | — | 覆盖 | — | 被覆盖 |
| **S3** | 覆盖 | 协同 | — | — | — | — | — | 协同 |
| **S4** | 协同 | — | — | — | 协同 | — | — | — |
| **S5** | 协同 | — | — | 协同 | — | — | — | — |
| **S6** | 协同 | 被覆盖 | — | — | — | — | — | 被覆盖 |
| **S7** | 协同 | — | — | — | — | — | — | — |
| **S8** | 覆盖 | 覆盖 | 协同 | — | — | 覆盖 | — | — |

**说明**：
- "协同"：两个策略一起工作，结果更稳健
- "覆盖"：后者触发时前者失效
- "被覆盖"：前者触发时后者失效

---

## 推荐启用顺序（落地节奏）

| 阶段 | 时间 | 启用策略 | 备注 |
|---|---|---|---|
| 阶段1 | 现在 → 1周 | S1 + S4 + S7 | 最小可用集，验证基本流程 |
| 阶段2 | 1周 → 1月 | + S2 + S3 | 加入风控，10万模拟盘跑1个月看回撤 |
| 阶段3 | 1月 → 2月 | + S5 + S6 | 信号分级和动态止盈，跑2个月看收益改进 |
| 阶段4 | 2月+ | + S8 | 黑天鹅应急，仅在极端行情才需要验证 |

**不要一次性全部启用**。先 S1 + S4 + S7 跑 1 周没问题再加 S2/S3，确保每层都验证过再叠下一层。

---

## 已知局限

- 阶段1的"流动性过滤"是粗略版，实盘需要更精确的滑点建模
- S8 的"监管黑天鹅"需要新闻API，目前模拟盘阶段先用大盘熔断条件
- S5 的打分项是基于回测经验，新市场环境可能需要重新校准
- 所有策略参数集中在 `config.json`，改完记得跑一次回测验证
